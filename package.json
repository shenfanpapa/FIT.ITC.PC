{
  "name": "roomcheck-server",
  "version": "1.5.1",
  "description": "教室設備管理システム",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node scripts/validate-data.js && node scripts/validate-defaults.js && node scripts/regression-check.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
