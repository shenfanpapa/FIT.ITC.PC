(()=>{if(matchMedia('(max-width:768px)').matches)return;let p=document.getElementById('whalePet');if(!p)return;let pass=localStorage.getItem('fit_pet_verified')||'',pw=document.createElement('form'),q=document.createElement('form'),b=document.getElementById('whalePetBubble');pw.id='whalePassword';q.id='whaleInput';pw.hidden=q.hidden=true;pw.innerHTML='<div class=pet-drag aria-label=drag></div><input type=password placeholder=合言葉 required><button>確認</button><button type=button>×</button>';q.innerHTML='<div class=pet-drag aria-label=drag></div><input placeholder=質問を入力 required><button>送信</button><button type=button>×</button>';document.body.append(pw,q);let close=f=>f.hidden=true;[pw,q].forEach(f=>f.querySelector('button[type=button]').onclick=()=>close(f));p.addEventListener('click',()=>{if(!pass){pw.hidden=false;pw.querySelector('input').focus()}else{q.hidden=!q.hidden;if(!q.hidden)q.querySelector('input').focus()}});pw.onsubmit=e=>{e.preventDefault();pass=pw.querySelector('input').value;localStorage.setItem('fit_pet_verified',pass);close(pw);b.textContent='今日は何を確認しますか？';b.classList.add('show')};q.onsubmit=async e=>{e.preventDefault();let i=q.querySelector('input'),s=q.querySelector('button');s.disabled=true;s.textContent='考え中…';try{let r=await fetch('/api/pet/chat',{method:'POST',headers:{'Content-Type':'application/json','x-pet-password':pass},body:JSON.stringify({message:i.value})}),j=await r.json();b.textContent=j.answer||j.message;b.classList.add('show');if(r.status===401){pass='';localStorage.removeItem('fit_pet_verified')}}finally{s.disabled=false;s.textContent='送信';close(q)}};document.addEventListener('keydown',e=>{if(e.key==='Escape'){close(pw);close(q)}})})();